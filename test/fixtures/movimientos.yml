# == Schema Information
#
# Table name: movimientos
#
#  id               :bigint           not null, primary key
#  monto            :decimal(10, 2)   default(0.0), not null
#  monto_flotante   :decimal(10, 2)   default(0.0), not null
#  saldo_actual     :decimal(10, 2)   default(0.0), not null
#  saldo_anterior   :decimal(10, 2)   default(0.0), not null
#  transaccion_type :string           not null
#  created_at       :datetime         not null
#  updated_at       :datetime         not null
#  cuenta_id        :bigint           not null
#  transaccion_id   :bigint           not null
#
# Indexes
#
#  index_movimientos_on_cuenta_id    (cuenta_id)
#  index_movimientos_on_transaccion  (transaccion_type,transaccion_id)
#
# Foreign Keys
#
#  fk_rails_...  (cuenta_id => cuentas.id)
#

one:
  transaccion: one
  transaccion_type: Transaccion
  cuenta: one
  monto_flotante: 9.99
  monto: 9.99
  saldo_anterior: 9.99
  saldo_actual: 9.99

two:
  transaccion: two
  transaccion_type: Transaccion
  cuenta: two
  monto_flotante: 9.99
  monto: 9.99
  saldo_anterior: 9.99
  saldo_actual: 9.99
